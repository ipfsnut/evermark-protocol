# Evermark Protocol Environment Configuration

# Core Application
NEXT_PUBLIC_APP_NAME='Evermark Protocol'
NEXT_PUBLIC_APP_VERSION='1.0.0'

# Deployment URLs
NEXT_PUBLIC_URL='http://localhost:3000'  # Local dev
NEXTAUTH_URL='http://localhost:3000'     # Local dev
# Production: https://evermark.epicdylan.com (Cloudflare Pages)

# Neynar/Farcaster Configuration
NEYNAR_API_KEY=''                        # Get from dev.neynar.com
NEXT_PUBLIC_NEYNAR_CLIENT_ID=''         # Neynar client ID
NEXT_PUBLIC_FARCASTER_DEVELOPER_FID=''  # Your Farcaster developer FID
FARCASTER_MINI_APP_ID=''                # Mini app ID

# Railway Services (managed externally via Railway CLI)
REDIS_URL=''                            # Railway Redis connection string
DATABASE_URL=''                         # Railway PostgreSQL (for everservice)

# Blockchain Configuration (Base L2)
NEXT_PUBLIC_THIRDWEB_CLIENT_ID=''       # Thirdweb client ID
NEXT_PUBLIC_CHAIN_ID='8453'             # Base network
NEXT_PUBLIC_WALLET_CONNECT_PROJECT_ID='' # WalletConnect project ID

# Smart Contract Addresses (Base network from beta)
NEXT_PUBLIC_EMARK_TOKEN_ADDRESS=''      # $EMARK token
NEXT_PUBLIC_WEMARK_ADDRESS=''           # Wrapped EMARK
NEXT_PUBLIC_EVERMARK_NFT_ADDRESS=''     # Evermark NFT contract
NEXT_PUBLIC_EVERMARK_VOTING_ADDRESS=''  # Voting contract
NEXT_PUBLIC_EVERMARK_LEADERBOARD_ADDRESS='' # Leaderboard
NEXT_PUBLIC_EVERMARK_REWARDS_ADDRESS='' # Rewards contract
NEXT_PUBLIC_FEE_COLLECTOR_ADDRESS=''    # Fee collector
NEXT_PUBLIC_MARKETPLACE_ADDRESS=''      # NFT marketplace
NEXT_PUBLIC_NFT_STAKING_ADDRESS=''      # NFT staking

# Storage Configuration (ArDrive + IPFS)
NEXT_PUBLIC_ARDRIVE_ENABLED='true'      # Enable ArDrive storage
ARDRIVE_PRIVATE_KEY=''                  # ArDrive wallet private key (server only)
ARDRIVE_WALLET_ADDRESS=''               # ArDrive wallet address
NEXT_PUBLIC_PINATA_JWT=''               # Pinata JWT for IPFS uploads
PINATA_API_KEY=''                       # Pinata API key (server only)
PINATA_SECRET_API_KEY=''                # Pinata secret (server only)

# Database (Supabase from beta)
NEXT_PUBLIC_SUPABASE_URL=''             # Supabase project URL
NEXT_PUBLIC_SUPABASE_ANON_KEY=''        # Supabase anon key
SUPABASE_SERVICE_KEY=''                 # Server-side only

# LLM Integration for Search & Tagging
OPENAI_API_KEY=''                       # OpenAI for search/tagging
ANTHROPIC_API_KEY=''                    # Anthropic Claude API

# Bot Configuration (for Neynar AI agent)
NEYNAR_WEBHOOK_SECRET=''                # Webhook verification
BOT_SIGNER_UUID=''                      # Bot signer UUID
BOT_FID=''                              # Bot Farcaster ID
BOT_USERNAME='emark-bot'                # Bot username
BOT_PRIVATE_KEY=''                      # Bot wallet private key
BOT_ADDRESS=''                          # Bot wallet address

# Feature Flags
NEXT_PUBLIC_SEASON_MANAGEMENT='true'    # Enable season management
NEXT_PUBLIC_COST_ESTIMATION='true'      # Show storage cost estimates
NEXT_PUBLIC_DUAL_STORAGE='true'         # Use both ArDrive and IPFS
NEXT_PUBLIC_ENABLE_VOTING='true'        # Enable voting features
NEXT_PUBLIC_ENABLE_STAKING='true'       # Enable staking features
NEXT_PUBLIC_ENABLE_MARKETPLACE='true'   # Enable NFT marketplace

# Development flags
NEXT_PUBLIC_DEBUG_MODE='true'           # Enable debug logging
USE_TUNNEL='false'                      # Use localtunnel for dev
NODE_ENV='development'

# Monorepo Architecture API Configuration
NEXT_PUBLIC_API_BASE_URL='http://localhost:3001'  # Local everservice (dev)
# Production: everservice deployed to Railway at custom domain
NEXT_PUBLIC_EVERSERVICE_URL=''          # Railway backend URL

# Security
NEXT_PUBLIC_MAX_FILE_SIZE_MB='25'       # Max file size for uploads
WEBHOOK_SECRET=''                       # General webhook secret